# Core dependencies for facial recognition with Oak D Series 3
depthai>=2.24.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0

# Face recognition
face-recognition>=1.3.0

# GUI
tk>=0.1.0

# 3D Scanning and Point Cloud Processing
open3d>=0.17.0  # Point cloud processing, mesh generation, visualization
trimesh>=4.0.0  # Advanced mesh operations, STL export, tool generation

# Gimbal Control (Feetech Servos)
pyserial>=3.5  # Serial communication for servo control

# Self-Learning and Reasoning System
scikit-learn>=1.3.0  # Machine learning algorithms for autonomous learning
cryptography>=41.0.0  # Encryption for secure memory storage (Gun.js style)

# Note: The following are included in Python standard library and don't need installation:
# - sqlite3 (for local database storage)
# - pickle (for serialization)
# - json (for configuration and data storage)
# - threading (for background processes)
# - socket (for peer-to-peer communication)
# - logging (for system logging)
# - dataclasses (for data structures)
# - enum (for enumerations)
# - collections (for deque and other data structures)
